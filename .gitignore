bin/
obj/
node_modules/
TestResults/
*.user
.DS_Store

# Local tooling configuration and working notes — not part of the product.
.claude/
CLAUDE.md


# Screenshot captures — regenerate with `cd src/Tript.Web && npm run shots`.
docs/shots/
# The settings file that run gives its host, so captures are always of a fresh install.
docs/shots-settings.json

# Frontend build artifacts (src/Tript.Web).
dist/
*.tsbuildinfo

# OBS bundle downloads / extraction (third_party/ is a build cache).
third_party/
