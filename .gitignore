bin/
obj/
node_modules/
*.user
.DS_Store

# Local tooling configuration and working notes — not part of the product.
.claude/
CLAUDE.md

# Model assets are large binaries; kept out of git pending a distribution decision.
data/models/**/*.onnx

# Frontend build artifacts (src/Tript.Web).
dist/
*.tsbuildinfo
