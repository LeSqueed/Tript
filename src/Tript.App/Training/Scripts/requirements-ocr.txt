# OCR recogniser fine-tune, driven by finetune_ocr.py through the vendored PaddleOCR checkout.
# The runner looks for this venv as .venv-paddle beside the app directory, not .venv:
#   py -3.12 -m venv .venv-paddle
#   .venv-paddle/Scripts/pip install -r requirements-ocr.txt
# Needs a Python the Paddle wheels cover (3.12 or 3.13). CPU only: PaddlePaddle publishes no
# ROCm or AMD build for Windows, so use_gpu stays false in en_PP-OCRv4_rec_finetune.yml.
Pillow>=10.0.0
PyYAML>=6.0
paddleocr>=3.0.0
paddlepaddle>=3.0.0
paddle2onnx>=2.0.0
onnx>=1.12.0,<2.0.0
